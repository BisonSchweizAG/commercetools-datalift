subprojects {
  apply plugin: 'java-library'
  java {
    sourceCompatibility = '21'
  }

  repositories {
    if (project.hasProperty('REPO1_URL')) {
      maven {
        url REPO1_URL
      }
    } else {
      mavenCentral()
    }

  }

}
